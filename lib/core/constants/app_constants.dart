class AppConstants {
  static const currencySymbol = 'GH₵';

  // Customer can checkout at any amount (no minimum).
  static const minOrderSubtotal = 0.0;
  static const baseDeliveryFee = 10.0;

  static const menuImagesBucket = 'menu-images';

  static const maxChatMessageLength = 2000;
}
